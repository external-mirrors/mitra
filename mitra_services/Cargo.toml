[package]
name = "mitra_services"
version = "2.20.1"
license = "AGPL-3.0-only"
edition = "2021"
rust-version = "1.63.0"

[dependencies]
# Used for catching errors
anyhow = "1.0.58"
# Used for working with dates
chrono = { version = "0.4.23", default-features = false, features = ["std"] }
# Used to work with hexadecimal strings
hex = { version = "0.4.3", features = ["serde"] }
# Used for logging
log = "0.4.20"
# Used to query Monero node
monero-rpc = { git = "https://github.com/silverpill/monero-rpc-rs", rev = "3462f23f8bee36fe2fd7312b85434c1090a5c95a", features = ["rpc_authentication"] }
# Used for working with regular expressions
regex = "1.9.6"
# Used for making async HTTP requests
reqwest = { version = "0.11.20", features = ["json", "multipart"] }
# Used for working with ethereum keys
secp256k1 = { version = "0.27.0", features = ["rand", "rand-std"] }
# Used for serialization/deserialization
# monero-rs is not compatible with serde 1.0.168+
serde = { version = "1.0.136,<1.0.168", features = ["derive"] }
serde_json = "1.0.89"
# Used to calculate SHA2 hashes
sha2 = "0.10.6"
# Used to verify EIP-4361 signatures
siwe = "0.5.2"
# Used for creating error types
thiserror = "1.0.37"
# Used to query ethereum node
web3 = { version = "0.19.0", default-features = false, features = ["http", "http-tls", "signing"] }

mitra_config = { path = "../mitra_config" }
mitra_models = { path = "../mitra_models" }
mitra_utils = { path = "../mitra_utils" }

[features]
default = ["native-tls"]

native-tls = ["reqwest/native-tls"]
rustls-tls = ["reqwest/rustls-tls-native-roots"]

[lints]
workspace = true
